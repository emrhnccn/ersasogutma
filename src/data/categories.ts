import { Category, Brand } from '@/types';

// Categories and brands are now fetched from MongoDB via API
// These serve as fallback defaults only
export const CATEGORIES: Category[] = [];
export const BRANDS: Brand[] = [];
